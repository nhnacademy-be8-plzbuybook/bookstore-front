#spring.application.name=bookstore-front

server:
  port: 3001

spring:
  application:
    name: bookstore-front1

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka